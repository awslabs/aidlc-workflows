{
  "version": "v1",
  "hooks": [
    {
      "name": "aidlc-write-audit-log",
      "trigger": "PostToolUse",
      "matcher": "^(write|fs_write|create_file|str_replace|fs_append|apply_patch|edit_file)$",
      "description": "Records every artifact you create or update in the workflow's audit log, then runs the checks that apply to that file.",
      "action": { "type": "command", "command": "{{INVOKE}} engine adapter kiro audit-and-sensors" }
    }
  ]
}

{
  "$schema": "https://raw.githubusercontent.com/aws/amazon-q-developer-cli/refs/heads/main/schemas/agent-v1.json",
  "name": "aidlc-architecture-reviewer-agent",
  "description": "AI-DLC Architecture Reviewer \u2014 delegation target for reviewing design artifacts for soundness and implementability.",
  "prompt": "file://aidlc-architecture-reviewer-agent.md",
  "tools": [
    "fs_read",
    "fs_write",
    "execute_bash",
    "thinking"
  ],
  "allowedTools": [
    "fs_read",
    "fs_write",
    "thinking"
  ],
  "toolsSettings": {
    "execute_bash": {
      "allowedCommands": [
        "bun (run )?[\"']?\\.kiro/tools/[A-Za-z0-9._-]+\\.ts[\"']?( .*)?",
        "bun (run )?[\"']?\\$\\{?KIRO_PROJECT_DIR\\}?/\\.kiro/tools/[A-Za-z0-9._-]+\\.ts[\"']?( .*)?",
        "bun (run )?[\"']?/[A-Za-z0-9 ._/-]+/\\.kiro/tools/[A-Za-z0-9._-]+\\.ts[\"']?( .*)?",
        "cd [^;&|<>$`()]+",
        "date -u( .*)?"
      ],
      "deniedCommands": [
        "rm -rf .*",
        "rm -fr .*",
        "git push.*"
      ]
    },
    "fs_write": {
      "allowedPaths": [
        "aidlc/spaces/**"
      ]
    }
  },
  "resources": [
    "file://.kiro/agents/aidlc-architecture-reviewer-agent.md",
    "file://.kiro/knowledge/aidlc-architecture-reviewer-agent/*.md",
    "file://.kiro/knowledge/aidlc-shared/*.md",
    "file://aidlc/spaces/default/memory/**/*.md"
  ],
  "hooks": {
    "preToolUse": [
      {
        "matcher": "fs_read",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts reviewer-scope aidlc-architecture-reviewer-agent",
        "timeout_ms": 15000
      },
      {
        "matcher": "fs_write",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts reviewer-scope aidlc-architecture-reviewer-agent",
        "timeout_ms": 15000
      },
      {
        "matcher": "execute_bash",
        "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts reviewer-scope aidlc-architecture-reviewer-agent",
        "timeout_ms": 15000
      }
    ]
  }
}

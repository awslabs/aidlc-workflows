# Fix Plan: Issues Found in PR #1 Post-Merge Review

**Date:** 2026-09-01
**Branch:** `feat/devin-harness`
**Status:** Approved (Plan mode)
**PR reviewed:** [#1 — feat(devin): release-engineering gates, harness resolution fallback, and two derived test rosters](https://github.com/wilmarques/aidlc-workflows/pull/1)
**Merged as:** `ad45d5de` (squash)

---

## Goal

Close four issues found in the post-merge review of PR #1 — one blocker (an
unasserted gate), one quality (a tautological assertion), one cosmetic (stale
comments/test name), and one policy (missing version bump) — in a single
commit on `feat/devin-harness`, returning the branch to a clean,
policy-compliant state.

## Scope

**In scope (4 files authored + regenerated dist):**

| File | Change |
|------|--------|
| `tests/unit/t238-build-binaries.test.ts` | Add `"harness-probe-devin"` to gate list |
| `tests/integration/t305-build-test-loopback-prose.test.ts` | Remove tautological count assertion |
| `tests/smoke/t250-question-fence-never-echo.test.ts` | Refresh header comment + test name for 8 harnesses |
| `core/tools/aidlc-version.ts` | Bump `2.6.125` → `2.6.126` |
| `CHANGELOG.md` | Add `## [2.6.126] - 2026-09-01` heading + bullets |
| `README.md` | Update version badge to `2.6.126` |
| `dist/<8 harnesses>/.../tools/aidlc-version.ts` | Regenerated by `package.ts` |

**Out of scope (prohibited changes):**

- Do NOT touch `scripts/build-binaries.ts` — the gate definitions are correct as merged.
- Do NOT touch `core/tools/aidlc-runtime-paths.ts` — the `.devin` fallback arm is correct as merged.
- Do NOT touch any `dist/<harness>/.../aidlc-runtime-paths.ts` — already correct as merged.
- Do NOT modify the t250 assertion logic — only the header comment and test name string are stale.
- Do NOT change the t305 per-harness sweep loop — only the count assertion above it is removed.
- Do NOT add new dependencies.
- Do NOT weaken or delete any existing test.
- Do NOT commit if no changes exist after regeneration.

## Context (inline, not imagined)

The merged PR added a `harnessProbeGate("devin", ...)` call to
`scripts/build-binaries.ts` (line ~1816), which creates a gate named
`harness-probe-devin`. The PR added `"runtime-devin"` to t238's asserted gate
list but omitted `"harness-probe-devin"`. The three sibling probe gates are
asserted at:

```
tests/unit/t238-build-binaries.test.ts:166  "harness-probe-kiro"
tests/unit/t238-build-binaries.test.ts:167  "harness-probe-copilot"
tests/unit/t238-build-binaries.test.ts:168  "harness-probe-opencode"
```

The t305 tautology compares `HARNESS_MATRIX.length` against
`readdirSync(harness).filter(manifest.ts exists).length`, but `HARNESS_MATRIX`
is built from that exact computation in `tests/harness/harness-matrix.ts:219-223`
and `:336`. The assertion can never fail.

The t250 header (lines 1-13) and test name (line 71) still say "six annexes"
after the roster was changed to derive from `HARNESS_MATRIX` (now 8 harnesses).

The repo's `AGENTS.md` Changelog Policy requires every user-visible PR to bump
`core/tools/aidlc-version.ts`, the README badge, and add a `## [X.Y.Z]` heading
to `CHANGELOG.md`. The `t68-version-changelog-sync.test.ts` guard enforces
agreement. The merged PR included no bump.

---

## Implementation (ordered sub-tasks — do each in sequence, verify before next)

### Sub-task 1: Fix t238 — add the missing probe gate assertion

**File:** `tests/unit/t238-build-binaries.test.ts`

**Action:** In the gate-name array (the `for (const name of [...])` block
starting ~line 160), insert `"harness-probe-devin"` immediately after
`"harness-probe-copilot"` and before `"harness-probe-opencode"`, preserving the
existing kiro → copilot → opencode alphabetical ordering.

**Exact target (current):**
```
      "harness-probe-kiro",
      "harness-probe-copilot",
      "harness-probe-opencode",
```

**Exact target (after):**
```
      "harness-probe-kiro",
      "harness-probe-copilot",
      "harness-probe-devin",
      "harness-probe-opencode",
```

**Verify before proceeding:** `grep -n "harness-probe-devin" tests/unit/t238-build-binaries.test.ts` returns exactly one match.

### Sub-task 2: Fix t305 — remove the tautological count assertion

**File:** `tests/integration/t305-build-test-loopback-prose.test.ts`

**Action:** Remove the `expect(HARNESS_MATRIX.length).toBe(readdirSync(...))`
block (lines ~563-569) entirely. Replace it with a single comment line noting
the sweep below is exhaustive over `HARNESS_MATRIX`, which is itself validated
against discovered manifests at import time in `tests/harness/harness-matrix.ts`.

**Do NOT** remove the `readdirSync` import if it is now unused — check with
`grep` first; if `readdirSync` has no other use in the file, remove it from the
import on line 36. If `existsSync` is also now unused, remove it too. Keep
`readFileSync`.

**Verify before proceeding:** `bun test tests/integration/t305-build-test-loopback-prose.test.ts` passes (the per-harness sweep loop is the real coverage and must still run over all 8 harnesses).

### Sub-task 3: Fix t250 — refresh stale header and test name

**File:** `tests/smoke/t250-question-fence-never-echo.test.ts`

**Action (3 edits):**

1. **Line 1 `// covers:` comment** — append the two missing harness doc refs:
   `doc:harness/devin/skills/aidlc/question-rendering.md(never-echo-spec)` and
   `doc:harness/copilot/skills/aidlc/question-rendering.md(never-echo-spec)`.

2. **Lines 7-12 annex enumeration** — add two lines:
   - `harness/devin/skills/aidlc/question-rendering.md     (numbered prose)`
   - `harness/copilot/skills/aidlc/question-rendering.md   (numbered prose)`

3. **Line 71 test name** — change `"all six annexes and the protocol exist..."`
   to `"all shipped annexes and the protocol exist..."` (drop the count, since
   the roster is now derived).

**Do NOT** change the `HARNESSES` constant, the assertion logic, or any other
test — those are correct as merged.

**Verify before proceeding:** `bun test tests/smoke/t250-question-fence-never-echo.test.ts` passes (all 8 harness annexes carry every required phrase — confirmed in review).

### Sub-task 4: Version bump + CHANGELOG + README badge

**Files:** `core/tools/aidlc-version.ts`, `CHANGELOG.md`, `README.md`

**Action (3 edits):**

1. **`core/tools/aidlc-version.ts`** — change `export const AIDLC_VERSION = "2.6.125"` to `"2.6.126"`.

2. **`CHANGELOG.md`** — insert a new heading immediately above the existing
   `## [2.6.125]` heading:

   ```markdown
   ## [2.6.126] - 2026-09-01

   Post-merge fixes for the devin release-engineering PR (#1): closes an unasserted build gate, removes a tautological test assertion, and refreshes a stale test header. **Upgrade:** no action required beyond re-copying `dist/devin/` if you use the compiled single-binary release; the runtime-paths and build-binaries changes from #1 are unchanged.

   * t238: added `"harness-probe-devin"` to the asserted gate list — the gate was added to `scripts/build-binaries.ts` in #1 but its pass/fail was never checked. The devin probe gate is now enforced alongside kiro, copilot, and opencode.
   * t305: removed a tautological count assertion (`HARNESS_MATRIX.length` vs the same computation it derives from). The per-harness sweep loop below it is the real coverage and is unchanged.
   * t250: refreshed the header comment and test name to reflect the 8-harness derived roster (was stale at "six annexes" after #1 switched from a hardcoded list).
   ```

3. **`README.md`** — update the version badge from `2.6.125` to `2.6.126`.

**Verify before proceeding:** `grep '"2.6.126"' core/tools/aidlc-version.ts` returns one match; `head -6 CHANGELOG.md` shows the new heading; `grep "2.6.126" README.md` returns one match.

### Sub-task 5: Regenerate dist + confirm parity

**Action:** Run `bun scripts/package.ts` to regenerate all 8 `dist/<harness>/`
trees (picks up the version bump in `aidlc-version.ts`), then run
`bun scripts/package.ts --check` to confirm zero drift.

**Do NOT** hand-edit any `dist/` file — the check command fails CI on drift.

**Verify before proceeding:** `bun scripts/package.ts --check` exits 0.

---

## Verification (completion criteria — run all, all must pass)

| # | Command | What it proves |
|---|---------|----------------|
| 1 | `bun scripts/package.ts --check` | Dist parity across 8 trees (version bump propagated) |
| 2 | `bun test tests/unit/t238-build-binaries.test.ts` | `harness-probe-devin` assertion present; gate list complete |
| 3 | `bun test tests/integration/t305-build-test-loopback-prose.test.ts` | Tautology removal didn't break the sweep |
| 4 | `bun test tests/smoke/t250-question-fence-never-echo.test.ts` | 8-harness sweep passes |
| 5 | `bun test tests/unit/t68-version-changelog-sync.test.ts` | Version/changelog/badge agreement (policy guard) |
| 6 | `bun run check` | Full typecheck + lint clean |

If any command fails, stop and fix before proceeding to the commit. Do not
commit with a failing verification step.

## Closeout (commit)

Single commit on `feat/devin-harness`, after all 6 verification commands pass:

```
git add tests/unit/t238-build-binaries.test.ts \
        tests/integration/t305-build-test-loopback-prose.test.ts \
        tests/smoke/t250-question-fence-never-echo.test.ts \
        core/tools/aidlc-version.ts \
        CHANGELOG.md \
        README.md \
        dist/
git commit -m "$(cat <<'EOF'
fix(tests): close t238 probe-devin gap, drop t305 tautology, refresh t250 header; bump 2.6.126

Post-merge fixes for PR #1 (commit ad45d5de):
- t238: assert harness-probe-devin (gate ran but was never checked)
- t305: remove tautological HARNESS_MATRIX.length count assertion
- t250: refresh stale "six annexes" header/test name to 8-harness derived roster
- bump 2.6.125 → 2.6.126 + CHANGELOG + README badge (AGENTS.md policy)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
EOF
)"
```

**Do NOT push unless explicitly asked.**

## Reporting format (when done)

Report back:
1. The exact `git show --stat HEAD` output (files changed + line counts).
2. The exit status of each of the 6 verification commands.
3. Any deviation from this plan and why.

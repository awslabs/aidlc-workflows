---
description: Set or change the AI-DLC workflow scope
---
Invoke the `aidlc` skill and pass the scope through to the orchestrator verbatim (e.g. `--scope <name>`): run `bun .cursor/tools/aidlc-orchestrate.ts next $ARGUMENTS`, then act on the single directive it returns.

$ARGUMENTS

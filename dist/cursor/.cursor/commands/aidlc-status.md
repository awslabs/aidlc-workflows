---
description: Show the current AI-DLC workflow status
---
Invoke the `aidlc` skill and pass `--status` through to the orchestrator: run `bun .cursor/tools/aidlc-orchestrate.ts next --status`, print its output verbatim, and stop.

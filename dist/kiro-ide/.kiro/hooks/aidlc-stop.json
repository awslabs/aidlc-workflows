{
  "version": "v1",
  "hooks": [
    {
      "name": "aidlc-stop",
      "trigger": "Stop",
      "description": "Enforces the forwarding loop — reminds the agent to continue when workflow has pending work.",
      "action": { "type": "command", "command": "bun .kiro/hooks/aidlc-kiro-adapter.ts stop" }
    }
  ]
}
